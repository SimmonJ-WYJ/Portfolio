# Solvely Stable Information Structure Design

## Goal

Add the static Figma `建立稳定的信息结构` showcase after the answer-understanding module on `/solvely-plugins`.

## Fixed Figma reference

- File: `y5i9X74zl66wfFNfGpMIAC`; node: `262:12747`.
- Stage: 1220 × 580 px, `#eef1f5`, 32 px radius.
- Three static Figma images: 255 × 484 px at x=175, x=482 and x=789; y=58; 10 px radius.
- Main flow arrow: x=228.17, y=19, 694.825 × 257.5 px.
- Short arrow: x=352, y=257, 128 × 5.7735 px.
- The original Figma SVG assets are mandatory for both arrows. They keep the original `#FF9292` path fill; no raster or hand-drawn replacement.
- Title: `建立稳定的信息结构`
- Body: `随着功能和场景不断增加，我将插件保持为稳定的三层结构：顶部承载全局控制，中间根据当前页面动态呈现任务和 AI 结果，底部保持输入与持续交互。这样无论用户处在 Canvas、YouTube 还是 PDF 中，内容虽然变化，但核心操作位置与信息层级始终一致。`

## Asset mapping

- Left entry panel: Figma original static image from node `262:12752`.
- Center answer/explanation panel: Figma original static image from node `262:12750`.
- Right summary panel: Figma original static image from node `262:12751`.

## Responsive rules

- Preserve absolute composition within a proportionally scaled 1220/580 stage.
- Retain 10 px image radius at desktop, 20 px stage radius at mobile and 16 px copy at mobile.
